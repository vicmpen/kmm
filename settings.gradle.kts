pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "testConf"
include(":androidTestConf")
include(":testConfShared")